<!DOCTYPE html>
<html>
  <head>
    <title>GovukTopicPagesPrototype</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body>
    <div id="wrapper">
      <%= render "govuk_component/beta_label", {} %>
      <%= yield %>
    </div>
  </body>
</html>
